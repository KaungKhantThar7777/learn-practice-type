import React from "react";
import { SpinnerContainer, SpinnerOverlay } from "../styled/Spinner";
const Spinner = () => {
  return (
    <SpinnerOverlay>
      <SpinnerContainer />
    </SpinnerOverlay>
  );
};

export default Spinner;
